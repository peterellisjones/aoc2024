use std::collections::HashSet;

use rayon::prelude::*;

use crate::{Day, utils::*};

pub struct DayXX;

impl Day for DayXX {
    const DAY_NUMBER: i64 = XX;
    const PART1_EXAMPLE_SOLUTION: i64 = -1;
    const PART2_EXAMPLE_SOLUTION: i64 = -1;

    fn part1(raw_input: &str) -> i64 {
        unimplemented!();
    }

    fn part2(raw_input: &str) -> i64 {
        unimplemented!();
    }
}
